using System.Diagnostics;
using UnityEngine;
using System.Collections;

public class callPython //: MonoBehaviour
{
/*
	Process compiler;
	// Use this for initialization
	void Start ()
	{
		compiler = new Process ();
		compiler.StartInfo.FileName = "py";
		compiler.StartInfo.Arguments = "C:\\Users\\julis\\Documents\\Development\\python\\test.py";
		compiler.StartInfo.UseShellExecute = false;
		compiler.StartInfo.RedirectStandardOutput = true;
		compiler.Start ();
	}

	// Update is called once per frame
	void Update ()
	{
			UnityEngine.Debug.Log(compiler.StandardOutput.ReadToEnd());
	}
	private void run_cmd(string cmd, string args)
{
     ProcessStartInfo start = new ProcessStartInfo();
     start.FileName = "C:/Program Files (x86)/Python35-32/python.exe";
     start.Arguments = string.Format("{0} {1}", cmd, args);
     start.UseShellExecute = false;
     start.RedirectStandardOutput = true;
     using(Process process = Process.Start(start))
     {
         using(StreamReader reader = process.StandardOutput)
         {
             string result = reader.ReadToEnd();
             Console.Write(result);
         }
     }
}
*/
}
